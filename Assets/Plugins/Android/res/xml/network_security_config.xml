<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- Permette HTTP non cifrato per tutti i domini -->
  <base-config cleartextTrafficPermitted="true" />
  <!-- Esempio di regola più restrittiva:
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">mio-dominio.local</domain>
  </domain-config>
  -->
</network-security-config>
